/**
 * @file Contains the AppController class.
 */

/**
 * @class AppController
 * @desc TODO AppController DESCRIPTION
 * @since v1.0.0
 */
const AppController = {

  /**
   * @desc Init the application.
   * @return {*}
   * @public
   */
  init (e) {
    // const menu = SpreadsheetApp.getUi()

    // menu.createMenu('GAS Webpack')
      // .addItem('Show Request', 'showRequestView') // global.showRequestView()
      // .addToUi()
  }
}

export default AppController
